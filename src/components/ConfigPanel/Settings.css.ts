import { style } from '@vanilla-extract/css'

export const aside = style({
  display: 'flex',
  position: 'fixed'
})

export const configBtn = style({
  all: 'unset',
  position: 'absolute',
  top: '1em',
  left: '1em'
})
